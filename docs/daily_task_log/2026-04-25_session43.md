# Session 43 — MaterialLocalizations 등록 → DateRangePicker 회귀 fix

> 일자: 2026-04-25
> 브랜치: `fix/material-localizations` → main squash merge (`164b60f`, PR #24)
> 베이스라인: Session 42 (`e26fe2d`) — 468 GREEN, analyze 0

## 미션

Session 41 monitor 로그에서 노출된 `No MaterialLocalizations found` assert 가 실기기 검증 중 사용자 액션(Search 탭 → 날짜 칩 탭)으로 트리거되어 빨간 ErrorWidget 출력. `MaterialApp.router` 에 `flutter_localizations` Global delegates 등록으로 회귀 차단.

## 변경 요약

### Code — App shell

- **`lib/app/app.dart`**
  - `LinkNoteApp` 에 static const 노출
    - `localizationsDelegates`: `Global{Material,Widgets,Cupertino}Localizations.delegate` 3종
    - `supportedLocales`: `[Locale('ko'), Locale('en')]`
    - `defaultLocale`: `Locale('ko')`
  - `MaterialApp.router` 파라미터에 적용
  - 테스트 가능 구조 (production 코드와 동일 const 참조 → 가짜 통과 위험 차단)

### Tests (TDD RED → GREEN)

- **신규 `test/app/app_localizations_test.dart`** — 3 cases
  - `exposes Global Material/Widgets/Cupertino delegates`
  - `supports ko and en locales with ko as default`
  - `MaterialLocalizations.of resolves ko without throwing` (widget test)
  - RED: const 미노출 상태에서 compile error 확인
  - GREEN: const 추가 후 3/3 통과

### Platform / 의존성

- **`pubspec.yaml`** — `flutter_localizations: { sdk: flutter }` 추가 (`flutter` SDK 직후, 알파벳 순)
- **`pubspec.lock`** — `flutter_localizations` 등록 + `intl 0.20.2` 트랜지티브 핀

## 검증

```
dart format lib/ test/                              # Formatted 306 files (0 changed)
flutter analyze --fatal-warnings                    # No issues found! (ran in 2.9s)
flutter test --reporter=failures-only               # +471: All tests passed! (+3 over baseline 468)
flutter run --flavor dev -t lib/main_dev.dart -d RFCW615RBFT
# 실기기(SM A346N, Android 16): Search → 날짜 칩 → DateRangePicker 빨간 화면 없이 정상 표시 ✅
```

## CI

PR #24 4 job ALL GREEN:
- Analyze ✅
- Test ✅
- Security Scan ✅
- Build (Android Dev Debug) ✅

## 별도 발견 (Out of Scope, 다음 세션 이월)

### Search 필터 단독 검색 트리거 안 되는 버그 (P1)

실기기 검증 중 별도 발견. 날짜 필터로 DateRangePicker 정상 동작 확인했으나 선택한 날짜 범위로 검색 결과 갱신이 안 됨.

**위치**: `lib/features/search/presentation/provider/search_provider.dart:120-125`

```dart
void _reSearchIfNeeded() {
  if (state.query.isNotEmpty) {  // ← 검색어 비면 통과
    state = state.copyWith(isSearching: true);
    _debouncer(() => _performSearch(state.query));
  }
}
```

**영향**: `setDateRange` / `toggleTagFilter` / `toggleFavoritesFilter` / `clearFilters` 모든 필터 변경 메서드. 검색어 없는 상태에서 필터 단독 적용 시 결과 갱신 안 됨.

**처리 방향 (다음 세션)**:
- `SearchLinksUseCase` 가 빈 쿼리 + 필터 입력을 어떻게 처리하는지 확인
- 빈 쿼리 + 필터 활성 시에도 검색 트리거하도록 가드 완화
- TDD: search_provider_test 에 RED 케이스 추가 (날짜 필터 단독 setDateRange → results 갱신)

### HomeScreen 빈 상태 한글화

Session 43 프롬프트의 Stage 2. Stage 1 격리 위해 본 PR 에서 분리. 다음 세션 처리.

## 메모리 갱신

- `project_code_review_roadmap.md` — Session 43 entry 추가 (Out of Scope 에 Search 필터 버그 기록)
- `MEMORY.md` — Code Review Roadmap / Design Overhaul 두 줄 갱신 (PR #24 추가)

## 세션 통계

- 변경 파일: 4 (`pubspec.yaml`, `pubspec.lock`, `lib/app/app.dart`, `test/app/app_localizations_test.dart`)
- 추가 줄: +80 / -0
- 테스트: 468 → 471 (+3)
- 실기기 검증: ✅ DateRangePicker / ❌ Search 필터 단독 (별도 PR 이월)

# Session 48 — Phase 4 LnTabBar 5탭→4탭 + 중앙 FAB

> 일자: 2026-05-01
> 브랜치: `feat/phase4-tabbar-center-fab` → squash merge `587cf86`
> 베이스라인: Session 47 (`25a63d5`) — 492 GREEN, analyze 0

## 미션

Phase 4 잔여 후보(LnTabBar 중앙 FAB / Dark mode forest 튜닝) 중 사용자 합의로 LnTabBar 단독 진행. 라우터 branch 재구성 수반(중간 회귀 위험)이므로 RED 테스트 우선.

## 시작 시 결정

- **Stage 1 실기기 시각 검증 스킵** — 사용자 요청 (4 세션 연속 스킵 상태).
- **Phase 4 후보 선택**: LnTabBar 중앙 FAB. Dark mode 는 차후 (golden test baseline 정책 합의 필요).
- **TDD 사이클**: `AppScaffoldWithNavBar.destinationLabels` 셀렉터 RED → GREEN.

## 변경 요약

### Code

**`lib/shared/widgets/app_scaffold_with_nav_bar.dart`**
- `NavigationBar.destinations` 5개 → 4개. Notifications destination 제거 → Home/Search/Collections/Profile.
- `Scaffold.floatingActionButton: FloatingActionButton(heroTag: 'shell_fab', backgroundColor: AppColors.forest, foregroundColor: Colors.white, elevation: 6, tooltip: '링크 추가', onPressed: () => context.push(Routes.linkAdd), child: Icon(Icons.add_rounded, size: 24))` 신설.
- `floatingActionButtonLocation: FloatingActionButtonLocation.centerDocked` — NavigationBar 위 중앙에 FAB 절반 오버랩.
- `static const List<String> destinationLabels = ['Home', 'Search', 'Collections', 'Profile']` 공개 — 테스트 셀렉터로 활용.

**`lib/app/router/app_router.dart`**
- 전역 `_notificationsNavKey` 제거.
- notifications `StatefulShellBranch` 제거.
- Notifications 를 top-level `GoRoute(path: Routes.notifications, parentNavigatorKey: _rootNavigatorKey, builder: (context, state) => const NotificationScreen())` 로 이전. `Routes.notifications` 경로 (`/notifications`) 자체는 변경 없음 — 호출 측 영향 없음.

**`lib/features/link/presentation/screens/home_screen.dart`**
- 로컬 `floatingActionButton`(home_fab, endFloat) 블록 + `floatingActionButtonLocation` 삭제 — shell 이 중앙 FAB 제공.
- bell `LnIconBtn(icon: Icons.notifications_none_rounded, badge: true, tooltip: '알림')` 의 no-op `onPressed: () {}` → `context.push(Routes.notifications)`.

**`lib/features/collection/presentation/screens/collection_list_screen.dart`** (변경 없음)
- shell add-link FAB(centerDocked, heroTag `shell_fab`) 와 collection add-collection FAB(endFloat, heroTag `collections_fab`) 는 위치/액션이 달라 시각/heroTag 충돌 없음.

### Tests

**신규 `test/shared/widgets/app_scaffold_with_nav_bar_test.dart`** (3 cases, RED→GREEN)
- `destinationLabels.length == 4` — 4탭 강제.
- `equals(['Home', 'Search', 'Collections', 'Profile'])` — 순서 강제.
- `isNot(contains('Notifications'))` — Notifications 부재 강제.

**`test/features/link/presentation/screens/home_screen_test.dart`** (1 case 기대 반전)
- "should show FAB for adding links" → "should NOT have its own FAB (shell provides central FAB)".
- `find.byType(FloatingActionButton)` `findsOneWidget` → `findsNothing`.

## 검증

- `dart format lib/ test/` — 변경 0.
- `flutter analyze --fatal-warnings` — 0 issues.
- `flutter test --reporter=failures-only` — 495 passed (492 → +3 net = shell test +3).

## TDD 사이클

1. **RED**: `test/shared/widgets/app_scaffold_with_nav_bar_test.dart` 작성 → `flutter test` 실행 → `Member not found: destinationLabels` 컴파일 에러 확인.
2. **GREEN**: `AppScaffoldWithNavBar.destinationLabels` 공개 + destinations 4개로 축소 + 중앙 FAB 추가 → 3 cases 통과.
3. **RED→GREEN**: `home_screen_test` FAB 기대 `findsNothing` 으로 반전.
4. 전체 `flutter test` → 495 GREEN.

## PR / 머지 흐름

- 푸시: `feat/phase4-tabbar-center-fab` → `870da25 feat(nav): 5탭 → 4탭 + 중앙 FAB, Notifications를 AppBar bell로 이동`.
- 직전 docs-only `1430938 docs(session47)` 함께 PR 본문에 묶임 (docs-only PR 금지 룰).
- PR #31 생성 → CI 통과 → 사용자 수동 머지 (`587cf86`, "Squash and merge").
- 로컬 main 동기화: `git reset --hard origin/main`. `feat/phase4-tabbar-center-fab` 브랜치 삭제.

## 메모리/문서 갱신

- `MEMORY.md` 인덱스 — Session 48 PR #31 entry 추가.
- `project_code_review_roadmap.md` — Session 48 완료 섹션 추가.
- `project_design_overhaul.md` — Phase 4 진척(PR #31) 표기 + Out of Scope 7 완료 처리.

## 인접 이슈 (별도 세션)

- 실기기 4 화면 톤 일관성 시각 검증 — **4 세션 연속 스킵 상태. 다음 실기기 가용 시 우선 처리 권장**.
- Phase 4 잔여: Dark mode forest 팔레트 튜닝 (golden test baseline 정책 합의 필요).
- Phase 2 iOS Share Extension.
- Supabase RLS / FK 점검.
- 정식 ARB 기반 intl 도입.
- Collection 디자인 (2열 그리드/그라디언트/Lock·Globe pill — 별도 Wave).

## 미완료 항목

- 실기기 시각 검증 (사용자 요청 스킵).

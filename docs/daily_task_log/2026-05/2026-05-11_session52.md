# Session 52 — Harness Sprint-1 ReadingStats 검증·머지 + dart format 자동화 권고

> 일자: 2026-05-11
> 브랜치: `harness/20260502-045500/sprint-1` (머지 후 정리됨)
> 베이스라인: Session 51 (`61e5c5b`) — 534 GREEN, analyze 0, alchemist golden 8개

## 미션

Session 51 끝나고 `harness/20260502-045500/sprint-1` 브랜치에 `aa3c396 wip(...NOT validated)` 커밋이 남아 있었음. 디스크에는 Mode B Opus + Codex AC-11 + Gemini AC-12 평가 산출물이 모두 존재했지만 commit 메시지/`sprint-1.json:cross_family_verdict` 필드가 stale. 이번 세션은 (A1) 독립 4차 재평가 → (A4) 산출물 정정 + main 머지의 4단계 시퀀스.

## 시작 시 결정

- 사용자가 **A1 → A4 순서** 선택. Round 4 독립 재평가 후 산출물 정정 + PR 머지까지 단일 흐름.
- Path B 부수효과(.g.dart 4파일, riverpod hash + envied XOR seed) **수락 결정**.
- Follow-up 영속 위치: **`docs/harness-followups.md`** (main 영속 + cross-sprint registry).
- 머지 전략: **squash** (저장소 `(#N)` suffix 컨벤션).

## 작업 흐름

### A1 — Round 4 독립 harness-evaluator 재평가
- `harness-evaluator` 에이전트(Opus) 독립 컨텍스트 생성. 기존 `sprint-1.json` / `sprint-1-mode-a.json` / cross-family md 미열람 강제(Independence Hard Rule).
- 결정적 검증 모두 self-executed: `flutter test test/features/reading_stats/` 16/16, `flutter test` 550/550, `flutter analyze` No issues, isolation grep 0, dart:mirrors 0, secret/HTTP/log-leak grep 0.
- **결과 `eval/sprint-1-round4-independent.json`**: PASS 73/80, Path B convergent. functional 33/35, security 14/15, quality 14/15, depth 12/15(repository_impl 단위 테스트 누락 -2, _writeQueue 누수 -1).
- 4-evaluator convergence 확정: Mode B 74 + Codex AC-11 PASS + Gemini AC-12 PASS + Round 4 73, 모두 Path B 동의.

### A2-1 — `sprint-1.json` artifact 정정
- `cross_family_verdict: "deferred"` → 실제 Codex/Gemini PASS 결과로 정정 (timestamp + 파일 ref 포함).
- `round4_independent_attestation` 신규 필드 추가 (재평가 evaluator/score/path/convergence note).
- JSON syntax 검증 통과.

### A2-2 — Commit 메시지 amend
- `aa3c396 wip(harness/sprint-1): ... NOT validated` → `a996f5a feat(reading_stats): Sprint-1 — domain + data + DI providers (4-evaluator PASS)`.
- 새 메시지에 4-evaluator score, deterministic verifier 결과, Path B 논거, 알려진 한계(handoff transparency) 포함. 사용자 사전 승인 후 amend.

### A3-1 — `docs/harness-followups.md` 신규 + Path B 수락
- F1~F9 9건 follow-up 영속 등록:
  - **F1**(low, out-of-scope) — Contract codegen exception 절 권고
  - **F2**(medium, in-contract) — AC-12 repository_impl 단위 테스트 갭
  - **F3**(low, in-contract) — `_writeQueue` identical() dead code
  - **F4**(low, in-contract) — AC-4/AC-13 ordering inversion
  - **F5**(low, in-contract) — Failure.message raw `$e` 미sanitize
  - **F6**(low, out-of-scope) — clock regression Sprint-2 연기
  - **F7**(low, out-of-scope) — O(n) read aggregation
  - **F8**(low, in-contract) — entity-level empty linkId 의도 허용
  - **F9**(low, out-of-scope) — `.gitignore` 추가
- Cross-sprint patterns 섹션(F1 자동 재발 / test parity gap / Mode A→B revision drift) 추가.
- 신규 commit `453d468 docs(harness): Sprint-1 observed follow-ups + cross-sprint patterns`.

### A3-2 — Push + PR + CI 사이클
- `git push -u origin harness/20260502-045500/sprint-1`.
- `gh pr create` → **PR #35** 생성.
- 1차 CI: **Analyze FAIL** (`dart format --set-exit-if-changed`로 reading_stats 테스트 2파일 wrap 누락 감지).
- 즉시 수정: `dart format lib/ test/` → 2 파일 reformat (logic 무변경). 검증 후 `0bc6602 style(...): dart format test files (CI Analyze fix)` push.
- 2차 CI: **4-job ALL GREEN** (Analyze/Test/Security Scan/Build Android Dev Debug).
- 사용자 수동 머지 → squash commit `7aa539d feat(reading_stats): Sprint-1 ReadingStats domain+data+DI (4-evaluator PASS) (#35)`.

### Post-merge cleanup
- 로컬 main sync (`git pull --ff-only`).
- 로컬 브랜치 삭제: `harness/20260502-045500/sprint-1`(squash 머지) + `feat/reading-stats-validation`(diff vs main empty, parallel attempt).
- MEMORY 갱신:
  - **신규 `project_harness_pipeline.md`** — harness 파이프라인 실전 운용 결과, Path B 선례, 4-evaluator convergence 의의, F1~F9 backlog 요약.
  - `MEMORY.md` index 3개 라인 갱신 (Code Review Roadmap에 PR #35 추가 / CI dart format 2회 재발 표기 / Harness Pipeline 신규 등록).
  - **`feedback_ci_dart_format.md` 강화** — Session 52 재발 사례 추가 + atomic 시퀀스 강제 + pre-push hook / harness deterministic_verifier 추가 권고.
- `tasks/lessons.md` 2026-05-11 항목 추가 — dart format 2회 재발 학습, Action items 백로그(pre-push hook / harness 검증 / 메모리→자동화 승격).

## 검증 결과

| 항목 | 결과 |
|------|------|
| `flutter test test/features/reading_stats/` | 16/16 PASS |
| `flutter test` (full) | 550/550 PASS, 0 regressions |
| `flutter analyze` | No issues found |
| `dart format --set-exit-if-changed lib/ test/` | clean (재실행 후) |
| `git diff main..HEAD -- lib/features/link/ pubspec.yaml lib/main.dart` | empty (격리 보존) |
| `grep features/link lib/features/reading_stats/` | 0 matches |
| `grep dart:mirrors lib/features/reading_stats/` | 0 matches |
| gitleaks (new files) | 0 leaks |
| CI 4-job | ALL GREEN (Analyze/Test/Security Scan/Build) |

## 핵심 교훈

1. **4-evaluator convergence 패턴 정착**. Mode B + cross-family Codex/Gemini + Round 4 independent의 4중 검증이 stress test 단계에서 valuable. 일반 운용은 Mode B + 1 cross-family로 충분할 가능성.
2. **Path B isolation judgment 선례**. zero behavioral delta 조건의 codegen 부수효과는 의무 follow-up과 함께 PASS 처리. 단, Contract 디자인 갭(`forbidden_files_codegen_exception` 절 부재)을 자동 재발 위험으로 인정 → F1로 등록.
3. **`docs/harness-followups.md`가 cross-sprint registry로 정착**. `.claude/harness/runs/*` 산출물은 gitignored → squash merge 후 자동 망각. 영속 도큐먼트가 다음 Planner에 도달하는 유일한 경로.
4. **`dart format` 누락 2회 재발**. Session 38 PR #18 lesson(메모리 entry 존재)에도 불구하고 재현. → 메모리만으론 재발 방지 부족, 자동 메커니즘(pre-push hook / harness deterministic_verifier) 승격 필요. `tasks/lessons.md` 2026-05-11에 강화 기록.

## 남은 잔여

- **`tasks/lessons.md` uncommitted modification** — L1 선택(다음 실코드 PR에 묶음).
- **F1 권고**(Contract codegen exception) 다음 Planner Contract 템플릿에 default 반영 필요. 미반영 시 Sprint-2도 동일 부수효과 재발.
- **F2**(AC-12 repository_impl 단위 테스트 갭, medium) — Sprint-1 touchup 또는 Sprint-2 preface로 closure 권고.
- **F3**(`_writeQueue` 누수) — Sprint-2 정리 권고.
- **실기기 시각 검증 8 세션 연속 스킵** — Session 53 1순위 후보.
- **Linux golden 재생성 deferred** — Phase 4.5 후속, workflow_dispatch.

## 머지 결과

- PR #35 머지: `7aa539d feat(reading_stats): Sprint-1 ReadingStats domain+data+DI (4-evaluator PASS) (#35)` (2026-05-11T12:55:03Z, squash).
- 28 files / +2312 / -408 (incl. generated `.freezed/.g.dart`).
- 분량의 ~80%는 freezed/riverpod codegen 산출물. 사람 손글씨 코드는 reading_stats 도메인/데이터 약 350줄 + 테스트 약 450줄 + storage_service 4줄.

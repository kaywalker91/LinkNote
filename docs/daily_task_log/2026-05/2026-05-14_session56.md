# Session 56 — Stabilization Sprint (PR #39 / #40 / #41)

> 일자: 2026-05-14
> 베이스라인: Session 55 (`621bf5e`) — 562 GREEN, analyze 0
> 최종: `5fe4fd2` — 566 GREEN, analyze 0, anti-pattern script PASS

## 미션

Session 55 의 Phase 4 마무리 후 중간 점검 + 안정화. Codex + Gemini 3-way 분석으로 누적 lesson (15건) 잔존 위반을 정량화하고, mechanical 패턴은 grep + CI step 으로 영구 차단. share_intent data layer / custom_lint 도입은 보류 (Phase 2 iOS 결정 의존 / rg script 대비 ROI 모호).

## 결과 요약

| PR | 제목 | Commit | risk profile |
|---|---|---|---|
| #39 | anti-pattern CI gate + highlight_text palette migration | `a0a1227` | 인프라 |
| #40 | `on Exception catch` → `on Object catch` (34 sites) | `1351849` | semantic |
| #41 | parseRows generic helper + propagation (3 datasources) | `5fe4fd2` | behavior |

- **테스트**: 562 → 566 GREEN (+4 `parseRowsTolerant` 단위 테스트)
- **anti-pattern 잔존**: AppColors 5→3(의도된 화이트리스트만), `on Exception catch` 34→0
- **회귀 차단**: `tool/check_anti_patterns.sh` 가 analyze job 에서 Category A+B 둘 다 FAIL 모드
- **format / analyze / test / script** 모두 GREEN

## PR #39 — anti-pattern CI gate + highlight_text palette migration

### 변경
- 신규 `tool/check_anti_patterns.sh` — grep 기반 카테고리 게이트
  - Category A (FAIL): `AppColors.<X>` 화이트리스트
  - Category B (WARN→FAIL): `on Exception catch` 카운트
- `.github/workflows/ci.yml` analyze job 에 step 연동 (format step 다음)
- `lib/shared/utils/highlight_text.dart` 의 마지막 잔존 2 site `AppColors.forestSoft/forestInk` 를 인자화 (`buildHighlightedSpans({highlightBg, highlightFg})`)
- 호출처 `lib/shared/widgets/ln/ln_link_card.dart:127` 가 `context.palette.forestSoft/forestInk` 전달
- 테스트 `test/shared/utils/highlight_text_test.dart` + `test/shared/widgets/ln/ln_link_card_highlight_test.dart` 갱신

### 의도된 화이트리스트 (잔존 3 site)
- `link_form_fields.dart:73` — `defaultTagColorHex` (데이터 hex)
- `app_snack_bar.dart:16-17` — success/error (Material semantic)
- 이전 메모리에 명시된 highlight_text 의 잔존은 PR #39 에서 마이그레이션 완료

## PR #40 — `on Exception catch` → `on Object catch` sweep

### 분포 (34곳)
- `og_tag_service.dart` ×1
- `auth_remote_datasource.dart` ×3
- `collection_remote_datasource.dart` ×5, `collection_local_datasource.dart` ×7
- `notification_remote_datasource.dart` ×3, `notification_local_datasource.dart` ×7
- `profile_remote_datasource.dart` ×2
- `search_remote_datasource.dart` ×1
- presentation 5곳: `link_list_provider:73`, `link_form_provider:223`, `home_screen:253`, `collection_list_provider:46`, `notification_list_provider:47`

### Caller signature 안전성
- presentation 3곳이 `e` 를 `loadMoreError` 로 전달. 해당 필드는 `lib/shared/models/paginated_state.dart:12` 와 `lib/shared/widgets/paginated_list_view.dart:26` 에서 이미 `Object?` 타입. 다운스트림 영향 없음.
- 나머지 31곳은 `Failure.X(message: e.toString())` 또는 `(_)` silent swallow — 변환 안전.

### Script 승격
- `tool/check_anti_patterns.sh` Category B 의 `ENFORCE=1` 로 flip. 회귀 시 CI FAIL.

## PR #41 — `parseRowsTolerant<T>` generic helper

### 신규
- `lib/core/utils/parse_rows.dart` — generic `parseRowsTolerant<T>(rows, parse, {label, onError})`
- `test/core/utils/parse_rows_test.dart` — 4 unit test (happy / skip+forward / **Dart Error subtype catch (StateError)** / empty)

### 적용
| Datasource | Method | 패턴 |
|---|---|---|
| `link_remote_datasource` | `parseRows` | thin wrapper (기존 시그니처 호환) |
| `collection_remote_datasource` | `getCollections` | 직접 호출 |
| `notification_remote_datasource` | `getNotifications` | 직접 호출 |
| `search_remote_datasource` | `fetchUserTags` | 직접 호출 (inline Tag mapper) |
| `search_remote_datasource` | `searchLinks` | `LinkRemoteDataSource.parseRows` 위임 → 자동 혜택 |

### 의도된 제외
- `profile_remote_datasource` — `getProfile` / `updateProfile` 단일 row 반환
- `auth_remote_datasource` — `signIn` / `signUp` / `signOut` 단일 row

per-row tolerance 의 적용 surface 없음. PR 본문에 명시.

## 검증 시퀀스 (각 PR 동일)

```bash
bash tool/check_anti_patterns.sh                  # PASS
dart format --set-exit-if-changed lib/ test/      # clean
flutter analyze --fatal-warnings                  # No issues found
flutter test                                      # 566 tests passed
```

## 메모리 갱신 (Task #4)

- `feedback_ci_dart_format.md` — PR #39 anti-pattern script 연동 사실 추가
- `feedback_dart_error_vs_exception.md` — PR #40 잔존 34곳 sweep + Category B 영구화 기록
- `feedback_per_row_parse_tolerance.md` — 표준이 `parseRowsTolerant<T>` 로 승격, 적용 현황·의도된 제외 명시
- `feedback_riverpod_async_notifier_inflight.md` — grep 시 `.g.dart` 포함 필요 사실 추가 (Session 56 발견)
- `MEMORY.md` 인덱스 — Code Review Roadmap + 4개 항목 hook 갱신, 35줄 (제한 200 내)

## Lessons (`tasks/lessons.md`)

- 2026-05-14 — 누적 lesson 의 회귀는 grep script + CI step 으로 영구 차단
- 2026-05-14 — 메모리 stale 판정은 grep 결과만 보면 오판한다
- 2026-05-14 — 안정화 sprint 는 risk profile 분리해 3+ PR 분할

## 후속 옵션

- **Sprint-3 진입** — reading_stats Home 카드 미니 배지 + harness 강화 (F-Sprint2-3 / F-Sprint2-4 Contract 템플릿 반영 후)
- **share_intent Phase 2** — iOS Share Extension
- **Linux golden 재생성** — `workflow_dispatch` 별도
- **Phase 5+ Lock/Globe pill UI**
- 보류된 anti-pattern script Category C 도입 후보:
  - freezed nested toJson `explicitToJson: true` 누락 검출 (mechanical 검출 가능한 형태인지 검증 필요)
  - `omit_local_variable_types` 위반 (이미 lint 가 잡고 있으면 중복)

## 관련 메모리

- `memory/project_code_review_roadmap.md` — Session 56 Stabilization 반영
- `memory/feedback_dart_error_vs_exception.md` — PR #40 sweep 기록
- `memory/feedback_per_row_parse_tolerance.md` — helper 승격 기록
- `memory/feedback_ci_dart_format.md` — script 연동 사실
- `tool/check_anti_patterns.sh` — 회귀 방지 게이트 본체

# Session 37 — Share Intent PRD Decided 승격 (Phase 1 착수는 Session 38 로 이월)

> 일자: 2026-04-21
> 브랜치: `docs/session37-share-intent-decided` (docs-only, PR 단독 금지 정책에 따라 Session 38 Phase 1 코드 PR 에 묶음)
> 베이스라인: Session 36 PR #17 (`0b2b59a`) — 437 GREEN, analyze 0

## 미션

`docs/prds/share-intent.md` 의 Open Decision 4건을 사용자와 합의해 **Decided** 로 승격. Phase 1 (Android URL-only PoC) 진입 준비 완료.

## 결정 합의 결과 (2026-04-21)

| # | 결정 항목 | 합의안 | 요약 사유 |
|---|---------|------|---------|
| 3.1 | Payload 타입 분기 | **URL only + `link/add` 폼 prefill** | 북마크 정체성 우선. plain text/image 는 엔티티 모델 확장이 필요해 Phase 3+ 이월 |
| 3.2 | 앱 상태별 동작 | **Cold start: GoRouter `initialLocation` 동적 분기 / Warm·Foreground: bottom sheet** | 표준 Flutter/deep-link 패턴. 풀스크린 강제 push 는 입력 손실 위험으로 배제 |
| 3.3 | iOS Share Extension | **Phase 2 이월** | Xcode·native Swift·App Groups·서명까지 범위 큼. Phase 1 은 Android 에 집중 |
| 3.4 | 패키지 선정 | **`receive_sharing_intent` 1.8.1** | iOS 15 / Android default minSdk 와 호환. PoC 속도 + Phase 2 iOS Extension 재사용 가치. 18개월 무업데이트는 Phase 2 재평가 포인트 |

## 변경 요약

### Docs

- **`docs/prds/share-intent.md` — Draft → Decided 승격**
  - 상단 상태 배너: `Draft` → `Decided (Session 37, 2026-04-21)` + Phase 1 진입 가능 명시
  - Section 3 각 하위 항목(3.1~3.4)에 `— **Decided (2026-04-21)**` 마크 + "결정" / "사유" / "구현 노트" (3.2 한정) 블록 추가
  - Section 3.1: 타입별 표를 Phase 1 대상(URL) 과 Phase 3+ 이월 타입으로 분리
  - Section 3.2: 부트 시퀀스 / warm·foreground 스트림 구독 / 작업 중 입력 보호 UX 규칙 명시
  - Section 3.3: Phase 2 진입 세션에서 재개할 "재검토 질문" 3건(Extension UI 범위 / Extension vs 메인앱 저장 / OG fetch 여부) 보존
  - Section 3.4: 패키지 호환성 실측치(pub.dev 조회 2026-04-21) 명시 — 1.8.1, 18개월 전, Android SDK 19+/iOS 12.0+
  - Section 7 결정 로그: 2026-04-21 의 5개 엔트리 추가(3.1~3.4 개별 결정 + PRD 상태 승격)
  - "다음 액션" 을 "Phase 1 Android URL-only PoC 구현 진입 — 구성 요소 5개" 로 갱신

### Code

- 없음 (본 세션 범위). Phase 1 PoC 는 Session 38 로 이월.

## Out of Scope

- **Phase 1 Android URL-only PoC 구현**: 최소 5+ 파일 변경(`pubspec.yaml` / `AndroidManifest.xml` / `bootstrap.dart` / `app_router.dart` / `link_add_screen.dart` + 도메인 서비스 + 테스트) + 실기기 검증 필요로 세션 경계 안에 안정 종료 판단 어려움. Session 38 로 이월.
- **iOS Share Extension / plain text URL 추출 / image / multi-payload**: PRD 결정에 따라 Phase 2~4 로 배치. 본 세션 범위 외.

## 검증

```
flutter analyze --fatal-warnings  # No issues found! (ran in 2.6s) — 세션 시작 baseline
flutter test --reporter=failures-only  # +437: All tests passed! — 세션 시작 baseline
```

> 본 세션은 docs-only 이므로 별도 테스트 증분 없음.

## 결과

- **Tests**: 437 GREEN (Session 36 동일 유지)
- **Analyze**: 0 issues
- **Files changed**: 1 (PRD) + 본 daily log + CHANGELOG + next-session-prompt 갱신 + MEMORY 항목 갱신
- **PR**: 미생성 (docs-only 단독 PR 금지 — Session 38 Phase 1 코드와 함께 묶음)

## 후속 (Session 38 작업 카드)

**미션**: Phase 1 Android URL-only PoC 완공 → 실기기 검증 → PR (Session 37 PRD 갱신 동반 커밋).

### Scope

1. `pubspec.yaml` — `receive_sharing_intent: ^1.8.1` 추가 후 `flutter pub get`
2. `android/app/src/main/AndroidManifest.xml` — `<intent-filter>` (`ACTION_SEND` + `text/plain` + `http/https` URL pattern) + optional `<data>` host filter
3. 도메인 서비스 `SharedIntentService` (신규) — payload 수신 getter + stream. `UrlSanitizer` 재사용해 URL 추출·검증
4. `lib/bootstrap.dart` 또는 `main.dart` — `runApp` 전 initial payload 조회 → GoRouter 에 전달(Provider override 또는 `initialLocation` 계산)
5. `lib/app/router/app_router.dart` — `Routes.linkAdd` 에 `?prefill=<encoded-url>` query 허용, initialLocation 을 payload 기준으로 동적 결정하거나 splash redirect 로직 확장
6. `lib/features/link/presentation/screens/link_add_screen.dart` — `prefill` 파라미터 수용 → 폼 URL 필드에 주입
7. Warm/foreground — app shell 에서 `receive_sharing_intent` stream 구독 → dirty 폼 보호(bottom sheet + "새 링크 저장" 스낵바) — Phase 1 후반/Phase 1 연장 검토
8. Tests (TDD):
   - `test/features/share_intent/domain/shared_intent_service_test.dart` — payload → URL 추출 성공/실패 케이스 (mock intent getter)
   - `test/app/router/app_router_test.dart` (있으면 확장) — prefill query 가 `LinkAddScreen` 초기값에 반영되는지 widget test
   - `test/features/link/presentation/screens/link_add_screen_test.dart` — `prefill` 파라미터 widget test
9. 실기기 / 에뮬레이터 검증 — YouTube/Chrome 공유 시트 → LinkNote 선택 → 폼 prefill 확인 (최소 2 앱 소스)
10. PRD 부가 갱신: Section 7 결정 로그에 "Phase 1 구현 착수 (Session 38)" entry 추가
11. PR 생성 — `feat(share-intent): Phase 1 Android URL-only PoC + PRD Decided` (Session 37 커밋 포함)

### 제약 / 원칙

- TDD RED → GREEN: `SharedIntentService` 도메인 레이어는 예외 없이 RED 선행
- ai-coding-pipeline: 5+ 파일 변경이므로 Plan 단계 선행 (Session 38 착수 시)
- i18n Option B: UX 신규 카피(bottom sheet/스낵바) 는 한글, 운영 exception/snackbar 는 영문
- `git push` / merge 사용자 명시 승인 필수 (불변 원칙)
- 수치 기준 창작 금지 — "안정화" 식 정성 표현 등장 시 `AskUserQuestion` 으로 확인

### Baseline

- main @ `0b2b59a` (Session 37 커밋은 `docs/session37-share-intent-decided` 로컬 브랜치에 보관)
- 437 tests GREEN, analyze 0

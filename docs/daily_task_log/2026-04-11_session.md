# 개발 세션 요약 — 2026-04-11

## 날짜 & 세션 정보

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-04-11 |
| 세션 파일 | 2026-04-11_session.md |
| 최신 커밋 | flutter analyze 0 issues + 315 tests ALL GREEN |
| 브랜치 | main |

---

## 쉽게 읽는 개발 일지 (비개발자용)

### 오늘 한 일을 한 줄로
Widget 테스트 보강 + Phase 5 Collection-Link 실 연동 + 보안감사 + UI/UX 개선 + Search 보강 + 릴리즈 준비 Phase 1 + 빌드 플레이버 분리 + 릴리스 서명 인프라 정비 + Critical Fix 3건, 315 테스트 ALL GREEN.

### 왜 했나요?
컬렉션 상세 화면이 Mock 데이터를 보여주고 있었기 때문에, 실제 Supabase API와 연동하여 진짜 링크 목록을 표시하도록 전환했습니다.

### 오늘 변경된 것 (쉬운 말로)
- Widget/Screen 테스트 16파일 추가 (95 테스트)
- 컬렉션 상세 화면에서 가짜 링크 목록 → 실제 API 데이터 연동
- 기존 링크 조회 기능에 "컬렉션별 필터링" 추가
- 컬렉션 캐시 코드의 잠재 버그 수정
- 전체 테스트 271개 → 283개 (+12), ALL GREEN

### 다음에 할 일
- 빌드 플레이버 분리 (dev/staging/prod) + Release Signing
- Firebase 연결 (flutterfire configure)
- 추가 UI/UX (OG 썸네일, 스와이프 제스처, Lottie)
- 오프라인 지원 강화

---

## 오늘 작업 요약

### 세션 #1: 이전 작업 커밋 + 다음 계획 수립
- **커밋 1** (`178f52e`): P1 — Profile/Notification data layer 구현 (29파일)
- **커밋 2** (`0eba95b`): P2 — OgTagService 캐싱 + Core/Repository 테스트 보강 (30파일)
- **커밋 3** (`34487c2`): docs — 세션 로그 + 다음 세션 프롬프트
- **커밋 4** (`63bcc5b`): docs — Widget 테스트 보강 계획 프롬프트

### 세션 #2: Widget/Screen 테스트 보강 구현
- **커밋 5** (`9b5a492`): test — Widget/Screen 테스트 16파일 추가 (95 tests, 전체 257 ALL GREEN)
  - Wave 1: Shared Widgets 6파일 (36 tests)
  - Wave 2: Feature Screens 5파일 (31 tests)
  - Wave 3: 나머지 Screens 5파일 (27 tests)

### 세션 #3: Phase 4 — Link 로컬 캐시 테스트 보강
- **커밋 6** (`bda183f`): docs — 세션 로그 + Phase 4 다음 세션 프롬프트

### 세션 #4: Phase 5 — Collection-Link 실 연동 + CollectionLocalDataSource 테스트
- Collection Links 실 연동 (6파일):
  - Domain: `ILinkRepository.getLinks()`, `FetchLinksUsecase.call()`에 `String? collectionId` 추가
  - Data: `LinkRemoteDataSource`에 Supabase `collection_id` 필터, `LinkLocalDataSource`에 collectionId 필터
  - Repository: `LinkRepositoryImpl`에 collectionId 전달 (remote + local fallback)
  - Presentation: `collection_links_provider.dart` — Mock 제거 → `@riverpod` + `FetchLinksUsecase` 실 연동
- 버그 수정: `CollectionLocalDataSource._mapToEntity`의 `on Exception` → `on Object`
- 테스트: `collection_local_datasource_test.dart` (신규 12 GREEN) + link_local_datasource collectionId 필터 테스트 1개 추가 + 기존 3 테스트 파일 mock 업데이트

검증: `flutter analyze` 0 errors, `flutter test` 283 ALL GREEN.

---

## 완료된 작업

- [x] Wave 1: Shared Widget 테스트 6개 작성 (36 GREEN)
- [x] Wave 2: Feature Screen 테스트 5개 작성 (31 GREEN)
- [x] Wave 3: 나머지 Screen 테스트 5개 작성 (27 GREEN)
- [x] flutter analyze 검증 (0 errors)
- [x] flutter test 검증 (257 ALL GREEN)
- [x] git commit + push (`9b5a492`)
- [x] 메모리 업데이트 (project_code_review_roadmap.md)
- [x] 문서 업데이트 + 다음 세션 프롬프트 생성
- [x] Phase 5: Collection-Link 실 연동 (6파일)
- [x] Phase 5: CollectionLocalDataSource 테스트 (12 GREEN)
- [x] Phase 5: collectionId 필터링 테스트 추가
- [x] Phase 5: _mapToEntity 버그 수정 (on Exception → on Object)
- [x] flutter test 검증 (283 ALL GREEN)
- [x] 문서 업데이트 (세션 로그, 다음 세션 프롬프트, 메모리)

### 세션 #5~#10: 보안감사 + UI/UX + Search + 릴리즈 준비
- **커밋 7** (`f5bf9cc`): 보안 감사 P1+P2 수정 — AuthInterceptor 401, signOut 캐시 정리, 글로벌 에러 핸들러
- **커밋 8** (`b089ab2`): UI/UX 개선 — 에러 한글화, 세션 만료 UX, 스켈레톤, Pull-to-Refresh
- **커밋 9** (`ce36726`): Android 패키지명 변경 + iOS 설정 업데이트 + 보안감사 문서
- **커밋 10** (`451fa31`): UI/UX 개선 Phase 2 — SnackBar 통합, 빈 상태 일러스트, 테마 전환 애니메이션
- **커밋 11** (`7abf337`): Search 기능 보강 — 필터/히스토리/자동완성 (303 tests)
- **커밋 12** (`b15031d`): docs — Search 보강 완료 다음 세션 프롬프트
- **커밋 13** (`b658aeb`): 릴리즈 준비 Phase 1 — 앱 아이콘, 스플래시, ProGuard, 메타데이터 통일

### 세션 #11: git push + 문서 업데이트
- 7개 미푸시 커밋 origin/main push 완료
- 315 tests ALL GREEN, flutter analyze 0 errors (31 info)
- 문서 업데이트 (workflow, changelog, session log, next session prompt)

### 세션 #12: CI 포맷 체크 수정
- **커밋 14** (`72e7547`): style — dart format 적용 (34개 테스트 파일)
- GitHub Actions CI에서 `dart format --set-exit-if-changed` 실패 → 수정 완료
- `dart format --set-exit-if-changed lib/ test/` 검증 통과 (0 changed)

### 세션 #14: 릴리스 서명 + 빌드 인프라 정비
- **Critical Fix 1**: `android/app/src/main/AndroidManifest.xml`에 INTERNET 퍼미션 추가 (릴리스 빌드 네트워크 차단 버그)
- **Critical Fix 2**: `ios/Runner.xcodeproj/project.pbxproj` — IPHONEOS_DEPLOYMENT_TARGET 13.0 → 15.0 (3곳)
- **Critical Fix 3**: `project.pbxproj` — Runner 타겟의 PRODUCT_BUNDLE_IDENTIFIER 하드코딩 삭제 → xcconfig flavor별 번들 ID 적용
- `ios/ExportOptions.plist` (신규) — App Store IPA 내보내기 템플릿
- `ios/Flutter/{Dev,Staging,Prod}-Release.xcconfig` — `DEVELOPMENT_TEAM=YOUR_TEAM_ID` 추가
- QA 테스트 플랜 수립: `.claude/plans/memoized-inventing-gadget.md` (릴리스 전용 오류 10항목 + 기능별 40+ 테스트 케이스)
- 메모리 업데이트: `project_release_signing.md` 신규 생성
- 검증: flutter analyze 0 issues, flutter test 315 ALL GREEN

### 세션 #13: flutter analyze --fatal-warnings 전체 수정
- `flutter analyze --fatal-warnings` 31개 이슈 → 0개 (No issues found!)
- 수정 범위 (16파일):
  - `Failure implements Exception` → only_throw_errors 5건 일괄 해결
  - 테스트 `any()` → `any<dynamic>()` / `any<Map<dynamic, dynamic>>()` 타입 인자 추가 (warning 8건)
  - cascade_invocations 5건 (context..snackBar()..pop() 패턴)
  - `unawaited()` 적용 (discarded_futures 3건)
  - `Text.new` tearoff + const 전파 (unnecessary_lambdas 5건)
  - raw string, 생성자 순서, 중복 인자 제거 등 기타 info 5건
  - 빌드 플레이버 관련 pre-existing 2건 (app_config.dart, dio_client.dart)
- 검증: `flutter analyze --fatal-warnings` 0 issues, `flutter test` 315 ALL GREEN

### 세션 #15: CI analyze info 0건 마무리 (only_throw_errors)
- **커밋 15** (`4b57674`): style(lint) — only_throw_errors 5건을 `Error.throwWithStackTrace`로 교체
- 배경: CI Flutter 3.41.4 환경에서 `Failure implements Exception`을 lint가 인식 못해 info 5건 잔존
- 수정 패턴 (기계적 1-라인 치환, Contract 범위 외 변경 없음):
  ```dart
  // Before
  throw result.failure!;
  // After
  Error.throwWithStackTrace(result.failure!, StackTrace.current);
  ```
- 근거: `Error.throwWithStackTrace`는 함수 호출 → only_throw_errors lint 대상 제외, 런타임 throw 동작 동일
- 수정 파일 (4파일 / 5건):
  - `collection_list_provider.dart:21`
  - `link_detail_provider.dart:15, :26`
  - `link_list_provider.dart:29`
  - `profile_provider.dart:16`
- 검증: `flutter analyze --fatal-warnings` 0 issues, `flutter test` 315 ALL GREEN, `dart format` 클린
- CI run `24278530380` 전체 통과 (Analyze 57s / Security 14s / Test 2m11s / Build 5m55s)

### 세션 #16: Firebase 연결 — Android Phase 1~4 (configure 완료, Dart 배선 대기)
- **배경**: next_session_prompt.md의 Option C (Firebase 연결) 진행. iOS는 의도적 제외, FCM도 다음 세션으로 연기.
- **결정 사항**:
  - 아키텍처: **단일 Firebase 프로젝트 `linknote-8994b` + flavor별 Android 앱 3개** (데이터 분리 vs 운영 단순 트레이드오프에서 단순성 우선)
  - 범위: Crashlytics + Analytics **초기화만**, FCM/Messaging dart 배선은 제외
  - 전략: dev PoC 선행 → staging/prod 확장
- **Firebase 콘솔 세팅 (사용자 수행)**:
  - 프로젝트 생성: `linknote-8994b` (display name "LinkNote")
  - Android 앱 3개 등록 — 패키지명 검증 완료:
    - `app.kaywalker.linknote.dev` → App ID `1:781978890801:android:dd9613381b6033ec3cdb10`
    - `app.kaywalker.linknote.staging` → App ID `1:781978890801:android:37bfa51063cb83bf3cdb10`
    - `app.kaywalker.linknote` → App ID `1:781978890801:android:aac32ebd6dd93ab13cdb10`
- **`flutterfire configure` 3회 실행 (비대화형, `--yes`)**:
  ```bash
  flutterfire configure \
    --project=linknote-8994b \
    --platforms=android \
    --android-package-name=<flavor-package> \
    --out=lib/firebase_options_<flavor>.dart \
    --android-out=android/app/src/<flavor>/google-services.json \
    --yes
  ```
  - dev / staging / prod 순서로 실행, 모두 "generated successfully"
- **flutterfire 자동 생성 / 수정 결과물**:
  - 신규 파일:
    - `lib/firebase_options_dev.dart`, `firebase_options_staging.dart`, `firebase_options_prod.dart`
    - `android/app/src/{dev,staging,prod}/google-services.json`
    - `firebase.json` (3 flavor buildConfigurations 모두 등록, Dart 섹션 3개 등록)
  - 수정 파일 (flutterfire 1차 실행 시 자동 배선):
    - `android/settings.gradle.kts` — `com.google.gms.google-services:4.3.15`, `com.google.firebase.crashlytics:2.8.1` 플러그인 선언 추가 (apply false)
    - `android/app/build.gradle.kts` — 두 플러그인 apply 추가
  - → **원래 수동 편집 예정이던 Android 네이티브 배선이 자동 완료됨**
- **테스트 영향 조사**: `test/`에서 `bootstrap` import 0건 확인 → `boot()` 시그니처 확장이 315 tests에 영향 없음 (다음 세션에서 안전 진행 가능)
- **커밋 3개 분리 기록**:
  - `6d7fe85` — `fix(release)`: Android INTERNET 퍼미션 + iOS 타겟 15.0 통일 + Runner 번들 ID 하드코딩 제거 + ExportOptions.plist 템플릿
  - `c422012` — `feat(firebase)`: 3 flavor Firebase configure (firebase_options_*.dart, google-services.json, firebase.json, gradle plugin 자동 배선)
  - `8400c9c` — `docs`: 세션 #16 요약 + CHANGELOG [Unreleased] + Session 17 프롬프트
- **Phase 5 (Dart 배선) 미실행** — 사용자 요청으로 문서 업데이트 우선 수행. Session 17에서 진행.
- **Push 미수행** — 4 커밋 로컬 대기, 사용자 명시적 승인 후 진행.

### 세션 #17: Firebase Android Phase 5 — Dart 배선 + 검증 완료
- **배경**: 세션 #16에서 Android Phase 1~4 (configure + Gradle plugin 자동 배선) 완료 후 Dart 런타임 배선만 남은 상태. 세션 #17은 ai-coding-pipeline Stage 4 (플랜 검증된 기계적 실행).
- **구현 (6파일)**:
  - `lib/bootstrap.dart` — `boot(AppEnv env, {required FirebaseOptions firebaseOptions})` 시그니처 확장. `Firebase.initializeApp(options: firebaseOptions)` 선행 호출, `setCrashlyticsCollectionEnabled(!kDebugMode)`, `FlutterError.onError`/`PlatformDispatcher.onError` 훅에 `recordFlutterError` / `recordError(fatal: true)` 추가 (기존 `appLogger.e` 호출 유지, `unawaited()` 래핑).
  - `lib/main_dev.dart` / `main_staging.dart` / `main_prod.dart` / `main.dart` — flavor별 `firebase_options_*.dart` import, `firebaseOptions: DefaultFirebaseOptions.currentPlatform` 전달.
  - `lib/app/router/app_router.dart` — `observers: [FirebaseAnalyticsObserver(analytics: FirebaseAnalytics.instance)]` 추가. StatefulShellRoute 탭 전환 한계는 주석으로 명시.
- **분석 이슈 해결**:
  - 1차 `flutter analyze` → `discarded_futures` 2건 (`recordFlutterError`/`recordError` 호출이 sync 핸들러 내부) → `unawaited()` + `dart:async` import 추가로 해결.
- **검증 결과** (순서 고정 준수):
  1. `flutter pub get` → resolved
  2. `dart run build_runner build --delete-conflicting-outputs` → 42 outputs (Riverpod codegen)
  3. `flutter analyze --fatal-warnings` → **0 issues**
  4. `flutter test` → **315 ALL GREEN** (10s)
  5. `dart format --set-exit-if-changed lib/ test/` → 274 files, 0 changed
  6. `flutter build apk --flavor dev -t lib/main_dev.dart --debug` → 57.4s, **성공** (Gradle plugin + google-services.json 정상 반영)
- **R1/R2/R3 리스크 전부 미발생**:
  - R1 (Gradle 버전 충돌) — `google-services:4.3.15` / `crashlytics:2.8.1` 이 Kotlin 2.2 / AGP 8.11과 호환됨
  - R2 (app_router.g.dart 변경) — codegen 결과 정상, 커밋 포함 대상
  - R3 (Analytics observer 한계) — 주석으로 기록, Option C로 연기
- **수동 스모크 테스트 (사용자 선택)**: 실기기/에뮬레이터 `flutter run --flavor dev` + Firebase DebugView `first_open` 수신 확인 — 세션 #18 또는 사용자 오프라인 실행으로 연기.
- **커밋 + Push**:
  - `0f441b8` — `feat(firebase)`: Android Crashlytics + Analytics Dart 배선 (Phase 5) — 15 files / +143 / -99 (bootstrap.dart, main_*.dart ×4, app_router.dart, .g.dart ×6, CHANGELOG, daily_log, next_session_prompt)
  - `git push origin main` → `4b57674..0f441b8 main -> main` (사용자 명시적 승인 후 수행)
  - 세션 #16 대기 4 커밋 + 세션 #17 1 커밋 = 총 5 커밋 원격 반영 완료

---

## 특이사항 / 메모

- InputChip의 delete 아이콘은 `Icons.cancel`이 아님 — `chip.onDeleted!()` 직접 호출로 해결.
- CircularProgressIndicator가 포함된 화면은 `pumpAndSettle` 대신 `pump()`를 사용해야 타임아웃 회피.
- Widget 테스트 패턴: Notifier 서브클래스 + `ProviderScope.overrideWith` (home_screen_test.dart Gold Standard).

---

_Generated by Claude Code · LinkNote Project_

# Session 58 — Track D (Linux goldens) + Track H (다크 모드 시각 검증)

> 일자: 2026-05-23
> 베이스라인: Session 57 (`9e15640`) — analyze 0 / format clean / anti-pattern PASS / non-golden test GREEN
> 환경: Flutter 3.41.4 (로컬을 3.38.3 → 3.41.4 업그레이드, ci.yml 매치) · Pixel 7 에뮬레이터 (다크 모드)

## 미션

1. **Track D** — Phase 4.5 잔여: Linux golden baseline 재생성 + CI golden 게이트 복원
2. **Track H** — Session 55 Track C palette migration 후속: 다크 모드 6화면 + Sprint-3 mini badge 시각 회귀 검증 (검증·기록 only, 코드 변경 0)

## 결과 요약

| Track | 산출 | 결과 |
|---|---|---|
| Baseline | Flutter 3.38.3 → 3.41.4 업그레이드 + `flutter clean` (kernel 캐시 불일치로 인한 17 tap-test 일시 실패 해소) | analyze 0 / format clean / anti-pattern PASS / non-golden test EXIT=0 |
| Track D — PR-D1 (#43) | `.github/workflows/regenerate-goldens.yml` 신규 (workflow_dispatch, ubuntu-latest) | CI 4/4 GREEN, merge `5fe47d1` |
| Track D — Linux 재생성 | `Regenerate Goldens` workflow main dispatch (run headSha `5fe47d1` == origin/main 검증) | artifact `goldens-ci-linux` 8 PNG 수령 |
| Track D — PR-D2 (#44) | golden 8 PNG 교체 + ci.yml `--exclude-tags golden` 제거 + deferred 주석 5줄 삭제 | CI Test job golden 8/8 Linux 검증 GREEN, merge `da4380b` |
| Track H | 다크 모드 7 대상 캡처 + 본 로그 | 시각 회귀 0 |

## Track D — 절차

- `workflow_dispatch` 는 default branch 머지 워크플로만 UI 트리거 가능 → PR-D1(workflow-only) 선행 분리
- macOS 로컬 golden 비교는 의도적으로 생략: `CI=true` 는 폴더만 전환하고 렌더러는 macOS Skia 그대로 → Linux PNG 와 역방향 pixel diff. golden 검증은 PR CI Test job(ubuntu-latest)에서만 수행
- PNG 8개 비전 육안 사전 검증 (link_card / collection_card / thumb 대표 확인 — 정상 렌더, 검은 화면·깨짐 없음)
- 8 PNG: `ln_brand` `ln_collection_card` `ln_icon_btn` `ln_link_card` `ln_segmented` `ln_tag` `ln_thumb` `ln_top_bar` (※ `ln_atoms_*` 는 미존재 — 이전 plan 오기재 확정)

## Track H — 다크 모드 시각 회귀 검증

> 방법: Pixel 7 에뮬레이터, 시스템 다크 모드 on (`cmd uimode night yes`), `adb exec-out screencap`. 자동 대비 측정 도구 미사용 — 색상 토큰 + 육안 가독성 + 다크 forest palette 정합 여부만 기술.

| 대상 | 회귀 | 색상 토큰 / 관찰 | 캡처 |
|---|---|---|---|
| Login | 없음 | wordmark(link ink / note forest), 필드 border `palette.line`, CTA `palette.forest`, 보조 텍스트 `ink3` — 모두 다크 bg 와 육안 구분 양호 | `login.png` |
| Search | 없음 | hint `ink3` 가독, 필터 pill 활성 시 `forest` filled + 체크(대비 우수), 결과 `LnLinkCard` host/title/tag/컬렉션/mini badge 정상 | `search.png` (+ `search_empty.png` 빈 상태 보조) |
| Collection Detail | 없음 | top bar edit/delete, `LnLinkCard` 리스트, 첫 카드 mini badge `1회 · 3분 전` 표시 | `collection_detail.png` (+ `collections_list.png` slate/amber 그라디언트 헤더 보조) |
| Edit | 없음 | title/desc/notes TextField, tag chip + delete, 즐겨찾기 toggle, 저장 CTA `forest` — 다크 정상 | `edit.png` |
| Form (Add) | 없음 | URL/제목/설명/메모/태그 필드 hint `ink3`, border `palette.line`, 저장 CTA `forest` | `form_add.png` |
| Skeleton | 없음 | `link_card_skeleton` shimmer placeholder(thumbnail + 타이틀 바 + 태그 chip)가 near-black bg 대비 미묘한 회색으로 충분히 구분됨 (network throttle 로 로딩 상태 강제 노출) | `skeleton.png` |
| mini badge | 없음 | `ReadingStatsBadge(compact: true)` = `1회 · 방금 전`, `AppTextStyles.caption + palette.ink3`. 카드 컬렉션명(`Missav`) 톤과 동등 가독성 — 과하지 않으면서 식별 가능 | `mini_badge.png` |

### 검증 데이터 셋업

- 첫 링크(`코하루 2`)를 롱프레스 → LinkDetail 진입으로 read 1회 기록 (`recordReadEventUsecase`, LinkDetail 화면에서만 발생 — Home 카드 탭은 URL 외부 오픈만 수행). totalReads 1 → mini badge 노출 조건(`totalReads > 0`) 충족 후 Home/Search/Collection Detail 3곳에서 일관 표시 확인

### 미캡처 / 한계

- 텍스트 키워드 하이라이트(`forestSoft`): 한글 IME 의 `adb input text` 입력 제약으로 별도 캡처 생략. 태그 필터 결과 카드 렌더는 정상 확인. 텍스트 매칭 하이라이트 다크 톤은 다음 검증으로 이월
- `defaultTagColorHex`(Form 신규 태그 잔여 light hex): 신규 태그 미입력으로 미관찰. 다크 가독성 별도 확인 필요 시 다음 트랙

## Closure

- Track D: PR-D1(#43) + PR-D2(#44) 머지 완료. CI golden 게이트 복원, Linux baseline 8/8 GREEN. Phase 4.5 잔여 종료
- Track H: 7 대상 다크 모드 시각 회귀 **0건**. Session 55 Track C palette migration + Sprint-3 mini badge 다크 정합 확인. 후속 트랙 불필요
- 본 PR 은 docs-only (캡처 + 로그). 시각 검증 산출물로 인정 (Session 36~37 워크플로 예외)

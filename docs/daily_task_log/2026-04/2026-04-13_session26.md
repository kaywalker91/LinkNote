# Session 26 — 2026-04-13

## Mission
Wave 2 코드 리뷰 P0/P1 수정 구현 (12건)

## Baseline
- main `2a45b6b` (353 tests GREEN, analyze 0)
- Branch Protection 활성화 상태

## Completed

### P0 수정 (4건 — Critical)

| ID | Fix | Files |
|---|---|---|
| P0-A | `IClearableCache` 인터페이스 → SignOutUsecase Clean Arch 위반 해소 | `i_clearable_cache.dart`(신규), `sign_out_usecase.dart`, 3x LocalDataSource, `auth_di_providers.dart` |
| P0-B | 미사용 `dio_client.dart` 삭제 → core→features 역참조 해소 | `dio_client.dart`(삭제), `dio_client.g.dart`(삭제) |
| P0-C | `collection_detail_provider` → 실제 `GetCollectionDetailUsecase` 호출로 교체 | `collection_detail_provider.dart` |
| P0-D | `Result.when()` success 콜백 `T` → `T?` (null-safe for Result<void>) | `result.dart` |

### P1 수정 (8건 — High)

| ID | Fix | Files |
|---|---|---|
| P1-A | `Routes.settings`: `/settings` → `/profile/settings` | `routes.dart` |
| P1-B | SearchHistory 전용 `search_history` Hive box 분리 | `storage_service.dart`, `search_di_providers.dart` |
| P1-C | P0-B에서 함께 해결 (dioProvider 삭제) | — |
| P1-D | OgTagService: `keepAlive: true` + `ref.onDispose(close)` | `og_tag_service.dart` |
| P1-E | AuthInterceptor: `VoidCallback?` → `Future<void> Function()?` + `unawaited` | `auth_interceptor.dart` |
| P1-F | link_form_provider: OG fetch 후 `state.value` 재읽기 (TOCTOU) | `link_form_provider.dart` |
| P1-G | connectivity_provider: `loading: () => false` (offline-safe) | `connectivity_provider.dart` |
| P1-H | AppSearchBar: clear 시 `_debouncer.dispose()` 호출 | `app_search_bar_widget.dart` |

### 테스트 변경
- `sign_out_usecase_test.dart`: MockClearableCache로 리팩터링
- `auth_interceptor_test.dart`: async 콜백 타입 반영
- `result_test.dart`: Result<void> when() 테스트 추가
- `dio_client_test.dart`: 삭제

### Metrics
- analyze: 0 issues
- tests: 352 GREEN (353 → 352, dio_client_test 삭제 -2, result_test 추가 +1)
- CI: 4/4 pass (Analyze, Test, Security Scan, Build)
- PR #7 merged to main (`42eed5e`)

### Branch Cleanup
- 로컬: `chore/session22-docs`, `chore/session24-docs`, `chore/wave2-review` 삭제
- 리모트: 위 3개 + `develop` 삭제, stale ref prune

## Next Session (27)
- Wave 3 코드 리뷰: Link feature (`lib/features/link/`)
- P2/P3 백로그는 Wave 3~6과 병행 처리

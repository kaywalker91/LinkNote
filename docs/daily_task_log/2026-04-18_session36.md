# Session 36 — Share Intent PRD 초안 + Collection/Search/Auth/url_launcher i18n 정리

> 일자: 2026-04-18
> 브랜치: `fix/wave3-i18n-collection-auth` (예정)
> 베이스라인: Session 35 PR #16 (`254c846`) — 437 GREEN, analyze 0

## 미션

Wave 5 Link 리뷰 로드맵 클로즈아웃의 마지막 조각:
1. Wave 5 P3-A 이월분(Share Intent) — PRD 초안 작성
2. Wave 3 P3-E i18n 확장 — Collection / Auth / url_launcher 의 사용자 대면 카피 정책 결정 및 적용

## 결정한 i18n 정책 (사용자 합의)

**Option B**: UI 카피(사용자 대면 hint/empty state/라벨) 는 한글 유지 + 개발자 / snackbar / Exception 메시지는 영문 통일.

- **근거**: 앱의 실 사용자 타겟이 한국어. UX 카피는 사용자 친화성 우선, 운영 메시지(snackbar/error)는 일관성과 향후 i18n 도입 시 분리 용이성 우선.
- **범위**: Search 화면의 hint/empty state/recent search 라벨은 한글 유지. snackbar 와 Exception 메시지만 영문화.
- **Auth 메시지**: 위 정책과 동일 적용 (signup 시 이메일 확인 안내 메시지는 snackbar 로 표시되므로 영문화 대상).

## 변경 요약

### Docs

- **신규 `docs/prds/share-intent.md`** — Wave 5 P3-A 이월 PRD 초안. 4개 선결 과제(payload 타입 분기 / 앱 상태별 동작 / iOS Share Extension / 패키지 선정) 를 옵션 / 미해결 질문 / 우선순위 / 비목표 / 결정 로그 구조로 정리. 본 문서는 Draft 상태이며 Share Intent Wave 진입 전 사용자 합의 필요.

### Code (snackbar / Exception 영문화)

- `lib/shared/utils/url_launcher_helper.dart` — 한글 snackbar 3건 → 영문
  - `'잘못된 링크 형식입니다'` → `'Invalid link format'`
  - `'링크를 열 수 없습니다'` → `'Cannot open link'`
  - `'링크를 여는 중 오류가 발생했습니다'` → `'Failed to open link'` (2 곳)
- `lib/features/collection/presentation/screens/collection_form_screen.dart` — snackbar 4건
  - `'컬렉션이 수정되었습니다'` / `'컬렉션이 생성되었습니다'` → `'Collection updated'` / `'Collection created'`
  - `'컬렉션 수정에 실패했습니다'` / `'컬렉션 생성에 실패했습니다'` → `'Failed to update collection'` / `'Failed to create collection'`
- `lib/features/collection/presentation/screens/collection_detail_screen.dart` — snackbar 2건
  - `'컬렉션이 삭제되었습니다'` → `'Collection deleted'`
  - `'삭제에 실패했습니다'` → `'Failed to delete collection'`
- `lib/features/auth/data/datasource/auth_remote_datasource.dart:67` — `Failure.auth` 메시지
  - `'이메일 확인 링크가 발송되었습니다. 메일을 확인하고 로그인해 주세요.'`
  - → `'A verification email has been sent. Please confirm your email and sign in.'`

### Tests (동시 갱신)

- `test/shared/utils/url_launcher_helper_test.dart` — 한글 메시지 4 expect → 영문
- `test/features/collection/presentation/screens/collection_detail_screen_test.dart:285` — `'삭제에 실패했습니다'` → `'Failed to delete collection'`
- `test/features/auth/data/datasource/auth_remote_datasource_test.dart:102` — `contains('이메일')` → `contains('verification email')`

## Out of Scope

- **Search 화면 사용자 대면 카피** (`'링크, 메모, 태그 검색'`, `'링크를 검색하세요'`, `'키워드를 입력하여 저장된 링크를 찾아보세요'`, `'최근 검색'`, `'전체 삭제'`, `'"$query" 검색 결과 없음'`): Option B 정책에 따라 한글 유지. 향후 정식 intl 도입 시 일괄 처리.
- **Share Intent 실 구현**: PRD Draft → 사용자 합의 → 별도 Wave 진입 후 구현.

## 검증

```
flutter analyze --fatal-warnings  # No issues found! (ran in 2.1s)
flutter test                       # +437: All tests passed!
```

## 결과

- **Tests**: 437 GREEN (Session 35 동일 유지)
- **Analyze**: 0 issues
- **Files changed**: 7 (lib 4 + test 3) + docs 신규 1 (PRD) + 본 daily log + CHANGELOG

## 후속

- Share Intent PRD 의 Open Decision 4건 → 사용자/스테이크홀더 합의 → Decided 항목 갱신 → Share Intent Wave 진입.
- Search 화면 한글 카피는 정식 intl(`flutter_localizations` + ARB) 도입 시 일괄 외부화 (별도 Wave 권장).
